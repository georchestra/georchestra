<?xml version="1.0" encoding="ISO-8859-1"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.georchestra</groupId>
        <artifactId>geoserver-root</artifactId>
        <version>14.12</version>
    </parent>
    <artifactId>geofence-webapp</artifactId>
    <packaging>war</packaging>
    <name>geOrchestra Geofence Web Application</name>
    <properties>
        <gf.unpack.dir>${project.build.directory}/geofence-unpacked-webapp</gf.unpack.dir>
    </properties>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.groovy.maven</groupId>
                <artifactId>gmaven-plugin</artifactId>
                <configuration>
                    <classpath>
                        <entry>
                            <groupId>org.georchestra</groupId>
                            <artifactId>config</artifactId>
                            <version>${project.version}</version>
                            <classifier>${server}</classifier>
                        </entry>
                    </classpath>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>2.4.1</version>
                <executions>
                    <execution>
                        <phase>validate</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-geofence-war</id>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <phase>generate-sources</phase>
                        <configuration>
                            <excludes>${geofence.war.excludes}</excludes>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>it.geosolutions.geofence</groupId>
                                    <artifactId>geofence-gui-web</artifactId>
                                    <version>${geofence.version}</version>
                                    <type>war</type>
                                </artifactItem>
                            </artifactItems>
                            <outputDirectory>${gf.unpack.dir}</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.2</version>
                <configuration>
                    <warSourceDirectory>${gf.unpack.dir}</warSourceDirectory>
                    <webXml>${gf.unpack.dir}/WEB-INF/web.xml</webXml>
                    <warName>geofence</warName>
                    <filter>${confdir}/${project.artifactId}/maven.filter</filter>
                    <webResources>
                        <resource>
                            <filtering>false</filtering>
                            <directory>${confdir}/${project.artifactId}</directory>
                            <excludes>
                                <exclude>maven.filter</exclude>
                                <exclude>README</exclude>
                            </excludes>
                        </resource>
                    </webResources>
                </configuration>
            </plugin>

        </plugins>
    </build>
</project>
