Configuration information is in src/<server_id>/ primarily maven.filter

Before a build one must create a directory in src/ to contain the configuration for the target server. Next one must create the configuration files.  See existing examples.

Next you can build.  The recommended method is to use cockpit/trunk/deploy.sh

cd cockpit/trunk/
./deploy.sh <server_id> extractorapp

the alternative is the following (execute in extractorapp/trunk)

mvn clean install -Dmaven.test.skip -Dserver=<server_id>
scp target/extractorapp-private.war <destination server>

