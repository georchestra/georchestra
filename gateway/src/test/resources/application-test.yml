georchestra.datadir: classpath:/test-datadir
georchestra.test-datadir: true #used to assert properties loaded from data directory
spring:
  cloud.gateway.routes:
  - id: testRoute
    uri: ${georchestra.gateway.target-mappings.header}
    predicates:
    - Path=/
    filters:
    - RewritePath=/, /header
    